# Lessons

Applied training lessons are added here **incrementally — one lesson per step**, each in its own
pull request. A lesson gets its own folder (named `NN-topic`) when it is actually implemented.

No lesson folders exist yet. Upcoming lessons are tracked in GitHub issues (label `training`).
